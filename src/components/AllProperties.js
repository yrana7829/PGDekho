import React from 'react';
import AllPropertiesCards from '../cards/PropertyCard'; // Import the PropertyCard component

const AllProperties = () => {
  return <AllPropertiesCards />;
};

export default AllProperties;
