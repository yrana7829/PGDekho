import React, { useState } from "react";
import SignInModal from "./SignInModal";

const SignInFormWithOTPModal = ({ handleClose }) => {
  return <SignInModal handleClose={handleClose} />;
};

export default SignInFormWithOTPModal;
